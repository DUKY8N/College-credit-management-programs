<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>과목 수</title>
    <link rel="stylesheet" href="/stylesheets/addSubject.css">
    <script>
        let date = "<%= date %>";
        let subjectCode = "<%= subjectCode %>";
        let scoreFilter = "<%= scoreFilter %>";
        let scoreSort = "<%= scoreSort %>";
        let scoreOrder = "<%= scoreOrder %>";
    </script>
    <script src="/javascripts/editSubject.js"></script>
</head>
<body
    data-date="<%= date %>"
    >
    <div class="window">
        <div class="title-bar">
            <span class="title"><span class="date"></span> 과목 수정</span>
        </div>
        <div class="addSubject-container">
            <form form class="addSubject-form">
                <label for="subject_code">과목 코드 (필수)</label>
                <input type="text" id="subject_code" name="subject_code" placeholder="COM302-01" oninput="this.value = this.value.replace(/[^0-9a-zA-Z-]/g, '').toUpperCase();" required>

                <label for="subject_name">과목 명 (필수)</label>
                <input type="text" id="subject_name" name="subject_name" placeholder="데이타베이스" required>
            
                <label for="academic_credit">과목 학점 (필수)</label>
                <input type="number" id="academic_credit" name="academic_credit" placeholder="3" oninput="this.value = this.value.replace(/[^0-9.]/g, '').replace(/(\..*)\./g, '$1');" required>
            
                <label for="grade">획득 평점 (선택)</label>
                <select id="grade" name="grade">
                    <option value="">아직 획득하지 않음</option>
                    <option value="4.5">A+</option>
                    <option value="4">A</option>
                    <option value="3.5">B+</option>
                    <option value="3">B</option>
                    <option value="2.5">C+</option>
                    <option value="2">C</option>
                    <option value="1.5">D+</option>
                    <option value="1">D</option>
                    <option value="0">F</option>
                </select>
            
                <button type="button" onclick="editScore()">수정하기</button>
            </form>
            <div class="back-link">
                <a href="/myGrades/<%=date %>/<%=scoreFilter%>/<%=scoreSort%>/<%=scoreOrder%>">돌아가기</a>
            </div>
        </div>
    </div>
</body>
</html>